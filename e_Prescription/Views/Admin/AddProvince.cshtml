@model e_Prescription.Models.Province

@{
    ViewData["Title"] = "AddProvince";
    Layout = "_Layout2";
}

<hr />
<div class="row">
    <div class="col-md-10">
        <form asp-action="AddProvince">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-row">
                <div class="form-group col-md-6">
                    <label asp-for="ProvinceName" class="control-label">Province name</label>
                    <input asp-for="ProvinceName" class="form-control" />
                    <span asp-validation-for="ProvinceName" class="text-danger"></span>
                </div>
            </div>
            <div class="d-flex justify-content-center">
                <input type="submit" value="Create" class="btn btn-success" />
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
